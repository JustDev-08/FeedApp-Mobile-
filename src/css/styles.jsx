import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    statusText: {
        fontSize: 20,
        fontStyle: 'italic',
        fontWeight: '700'
    },
    inputText: {
        padding: 10,
        fontStyle: 'italic',
        fontWeight: 'bold',
    }
})

export default styles